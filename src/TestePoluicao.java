public class TestePoluicao {
    /* ESTARIA TUDO NA CLASSE FUNCIONÁRIO, PORÉM DEIXA O CÓDIGO POLUÍDO
    private int tipo = 0; //0 = Funcionário comum, 1 = Gerente, 2 = Diretor, 3 = Presidente

    public int getTipo() {
        return tipo;
    }

    public void setTipo(int tipo) {
        this.tipo = tipo;
    }

    public double getBonificacao() {
        if (this.tipo == 0) {//0 = Funcionário comum
            return this.salario * 0.1;
        } else if (this.tipo == 1) {//1 = Gerente
            return this.salario;
        } else if (this.tipo == 2) {//2 = Diretor
            return this.salario * 1.5;
        } else if (this.tipo == 3) {//3 = Presidente
            return this.salario * 3;
        }else {
            return 0;
        }
    }
*/
}
